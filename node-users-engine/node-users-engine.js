#!/usr/bin/env node

'use strict';

const program = require('commander');
program
  .version('0.1')
  .command('')
  .description('')
  .option('','')
  .option('','')
  .action('');
program.parse(process.argv);
